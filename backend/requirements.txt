# API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Data validation & settings (FastAPI uses Pydantic v2)
pydantic>=2.0
pydantic-settings>=2.0

# OpenAI Python SDK ("opensdk")
openai>=1.40.0

# Vector store
chromadb>=1.5.0

# SQLite: use the standard library `sqlite3` module — no PyPI package.
# Optional later for async DB access: aiosqlite + SQLAlchemy, etc.
